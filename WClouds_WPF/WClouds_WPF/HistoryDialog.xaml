<Window x:Class="WClouds_WPF.HistoryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Verlauf" Height="420" Width="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Background="#1E1E2E">

    <Window.Resources>
        <SolidColorBrush x:Key="BgBrush"     Color="#1E1E2E"/>
        <SolidColorBrush x:Key="PanelBrush"  Color="#2A2A3E"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#7C6AF7"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#E0E0F0"/>
        <SolidColorBrush x:Key="TextMuted"   Color="#7A7A9A"/>
        <SolidColorBrush x:Key="HoverBrush"  Color="#33334A"/>

        <Style TargetType="ListView">
            <Setter Property="Background"      Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground"      Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontFamily"      Value="Segoe UI"/>
            <Setter Property="FontSize"        Value="13"/>
        </Style>
        <Style TargetType="ListViewItem">
            <Setter Property="Background"                    Value="Transparent"/>
            <Setter Property="Foreground"                    Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontFamily"                    Value="Segoe UI"/>
            <Setter Property="FontSize"                      Value="13"/>
            <Setter Property="Padding"                       Value="4,5"/>
            <Setter Property="BorderThickness"               Value="0"/>
            <Setter Property="HorizontalContentAlignment"    Value="Stretch"/>
            <Style.Triggers>
                <Trigger Property="IsSelected"  Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource HoverBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="GridViewColumnHeader">
            <Setter Property="Background"                 Value="#242436"/>
            <Setter Property="Foreground"                 Value="{StaticResource TextMuted}"/>
            <Setter Property="FontFamily"                 Value="Segoe UI Semibold"/>
            <Setter Property="FontSize"                   Value="11"/>
            <Setter Property="Padding"                    Value="6,4"/>
            <Setter Property="BorderThickness"            Value="0,0,0,1"/>
            <Setter Property="BorderBrush"                Value="{StaticResource AccentBrush}"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>
        <Style x:Key="DlBtn" TargetType="Button">
            <Setter Property="Background"      Value="#33334A"/>
            <Setter Property="Foreground"      Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontFamily"      Value="Segoe UI"/>
            <Setter Property="FontSize"        Value="11"/>
            <Setter Property="Height"          Value="24"/>
            <Setter Property="Padding"         Value="10,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#7C6AF7"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Background" Value="#22223A"/>
                                <Setter Property="Foreground" Value="#4A4A6A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock x:Name="TitleText"
                       Foreground="{StaticResource AccentBrush}"
                       FontFamily="Segoe UI Semibold" FontSize="15"/>
            <TextBlock x:Name="SubText"
                       Foreground="{StaticResource TextMuted}"
                       FontFamily="Segoe UI" FontSize="11" Margin="0,2,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Background="{StaticResource PanelBrush}" CornerRadius="10" Padding="8">
            <ListView x:Name="HistoryList" SelectionMode="Single">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Datum / Uhrzeit" Width="150"
                                        DisplayMemberBinding="{Binding DateTimeDisplay}"/>
                        <GridViewColumn Header="Größe" Width="90"
                                        DisplayMemberBinding="{Binding SizeDisplay}"/>
                        <GridViewColumn Header="Geändert von" Width="200"
                                        DisplayMemberBinding="{Binding ChangedUser}"/>
                        <GridViewColumn Header="Backup" Width="110">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="⬇ Herunterladen"
                                            Style="{StaticResource DlBtn}"
                                            IsEnabled="{Binding HasBackup}"
                                            Tag="{Binding}"
                                            Click="DownloadBackup_Click"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>

        <TextBlock x:Name="StatusText" Grid.Row="2"
                   Foreground="{StaticResource TextMuted}"
                   FontFamily="Segoe UI" FontSize="11"
                   Margin="0,8,0,0" Text="Lade Verlauf…"/>
    </Grid>
</Window>
